% \subsection{Застосування Mediapipe від Google для отрмання маски людини}

% Введемо поняття \textbf{маски людини (викладача)}.

% Маскою людини \(F^{i}\) будемо називати бінарне
% зображення \(M^{i}:P \rightarrow \left\{ 0,1 \right\}\), де тим
% пікселям, в яких на відповідному кадрі \(F^{i}\) був помічений викладач,
% відповідає одиниця, а іншим відповідає нуль.

% \subsection{Mediapipe}

% Mediapipe - це 